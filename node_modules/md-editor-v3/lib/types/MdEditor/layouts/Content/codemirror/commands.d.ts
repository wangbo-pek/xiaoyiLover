import { ContentProps } from '../props';
import { KeyBinding } from '@codemirror/view';

declare const createCommands: (id: string, contentProps: ContentProps) => KeyBinding[];
export default createCommands;
