import { ContentPreviewProps } from '../ContentPreview';
import { Ref } from 'vue';

/**
 * 放大图片
 *
 * @param props 基础属性
 * @param html 编译后的html
 */
declare const userZoom: (props: ContentPreviewProps, html: Ref<string>) => void;
export default userZoom;
